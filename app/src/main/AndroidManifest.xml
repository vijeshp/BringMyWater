<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="mywater.mywater">

    <uses-permission android:name="android.permission.ACCESS_FINE_LOCATION" />

    <application
        android:allowBackup="true"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:supportsRtl="true"
        android:theme="@style/AppTheme">
        <activity android:name=".BaseActivity" />
        <activity android:name=".SplashScreen" />
        <activity
            android:name=".order.OrderActivity"
            android:label="@string/title_activity_order"
            android:screenOrientation="portrait" />
        <activity
            android:name=".Authentication.Signup"
            android:label="@string/title_activity_signup"
            android:theme="@style/AppTheme.NoActionBar" />
        <activity
            android:name=".Authentication.LoginActivity"
            android:label="@string/title_activity_login">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
        <activity
            android:name=".HomeActivity"
            android:label="@string/title_activity_home"
            android:theme="@style/AppTheme.NoActionBar" />

        <uses-permission android:name="android.permission.INTERNET" />
        <uses-permission android:name="android.permission.ACCESS_NETWORK_STATE" />
        <uses-permission android:name="android.permission.ACCESS_FINE_LOCATION" />

        <meta-data
            android:name="com.google.android.geo.API_KEY"
            android:value="AIzaSyC6tdN7qrnK0ypcyUpX5yhVsk_0oWhlKKY" />

        <activity
            android:name=".Authentication.SignUpBaseActivity"
            android:label="@string/title_activity_sign_up_base"
            android:theme="@style/AppTheme.NoActionBar" />
        <activity android:name=".Authentication.SignUpActivity" />
        <activity android:name=".Authentication.SignupData" />
        <activity android:name=".Authentication.OtpActivity" />
        <activity
            android:name=".common.AddressScreen"
            android:label="@string/title_activity_address_screen"
            android:theme="@style/AppTheme.NoActionBar"></activity>
    </application>

</manifest>